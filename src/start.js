    /* var sphere = BABYLON.MeshBuilder.CreateSphere("sphere", {
        diameter: 2
    }, scene);

    sphere.material = myMaterial;
    sphere.position = new BABYLON.Vector3(0, 0, 0); */

    //sphere.checkCollisions = true;

    //raycasts - Kollisionen zwischen Meshes und unendlichen Linien

    /* scene.registerBeforeRender(function () { //jeden Frame
        if (balloon.intersectsMesh(sphere, false)) { //Mesh, Präzise?
            console.log("jo1");
        } else {
            console.log("jo2");
        }
    }); */